<Project Sdk="Microsoft.NET.Sdk">
  <!-- Metadata -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <Product>FurkanTural.Labs.Lab21</Product>
    <Description>Proof for blog post #21 — how many requests actually reach the endpoint under each caching approach.</Description>
  </PropertyGroup>

  <!-- Assembly information -->
  <PropertyGroup>
    <AssemblyTitle>FurkanTural.Labs.Lab21</AssemblyTitle>
    <AssemblyDescription>Counts endpoint invocations behind ResponseCache headers, response caching and output caching.</AssemblyDescription>
    <RootNamespace>Lab_21_OutputCaching</RootNamespace>
  </PropertyGroup>

  <!-- Versioning -->
  <PropertyGroup>
    <Version>1.0.0</Version>
  </PropertyGroup>

  <!-- Project References -->
  <ItemGroup>
    <ProjectReference Include="..\..\Signature\FurkanTural_Labs_Application\FurkanTural_Labs_Application.csproj" />
    <ProjectReference Include="..\..\Web\FurkanTural_Labs_Host\FurkanTural_Labs_Host.csproj" />
  </ItemGroup>
</Project>
