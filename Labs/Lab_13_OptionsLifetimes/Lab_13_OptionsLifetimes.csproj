<Project Sdk="Microsoft.NET.Sdk">
  <!-- Metadata -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <Product>FurkanTural.Labs.Lab13</Product>
    <Description>Proof for blog post #13 — which of the three options interfaces actually re-reads a changed file.</Description>
  </PropertyGroup>

  <!-- Assembly information -->
  <PropertyGroup>
    <AssemblyTitle>FurkanTural.Labs.Lab13</AssemblyTitle>
    <AssemblyDescription>Changes a setting on disk at runtime and records the number each consumer reads afterwards.</AssemblyDescription>
    <RootNamespace>Lab_13_OptionsLifetimes</RootNamespace>
  </PropertyGroup>

  <!-- Versioning -->
  <PropertyGroup>
    <Version>1.0.0</Version>
  </PropertyGroup>

  <!-- NuGet Packages -->
  <!-- Bu laboratuvar sunucu kurmaz: ölçtüğü şey konfigürasyon okuma biçimi, boru hattı değil. -->
  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" Version="10.0.0" />
    <PackageReference Include="Microsoft.Extensions.Configuration.Json" Version="10.0.0" />
    <PackageReference Include="Microsoft.Extensions.Options.ConfigurationExtensions" Version="10.0.0" />
  </ItemGroup>

  <!-- Project References -->
  <ItemGroup>
    <ProjectReference Include="..\..\Signature\FurkanTural_Labs_Application\FurkanTural_Labs_Application.csproj" />
  </ItemGroup>
</Project>
