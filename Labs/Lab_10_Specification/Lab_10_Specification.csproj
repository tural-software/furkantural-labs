<Project Sdk="Microsoft.NET.Sdk">
  <!-- Metadata -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <Product>FurkanTural.Labs.Lab10</Product>
    <Description>Proof for blog post #10 — the Specification pattern and the Func/Expression trap.</Description>
  </PropertyGroup>

  <!-- Assembly information -->
  <PropertyGroup>
    <AssemblyTitle>FurkanTural.Labs.Lab10</AssemblyTitle>
    <AssemblyDescription>Measures how many rows leave the database when a specification carries a delegate instead of an expression.</AssemblyDescription>
    <RootNamespace>Lab_10_Specification</RootNamespace>
  </PropertyGroup>

  <!-- Versioning -->
  <PropertyGroup>
    <Version>1.0.0</Version>
  </PropertyGroup>

  <!-- Content -->
  <ItemGroup>
    <None Update="appsettings.json">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
  </ItemGroup>

  <!-- Project References -->
  <ItemGroup>
    <ProjectReference Include="..\..\Signature\FurkanTural_Labs_Application\FurkanTural_Labs_Application.csproj" />
    <ProjectReference Include="..\..\Infrastructure\FurkanTural_Labs_Persistence\FurkanTural_Labs_Persistence.csproj" />
  </ItemGroup>
</Project>
