<Project Sdk="Microsoft.NET.Sdk">
  <!-- Metadata -->
  <PropertyGroup>
    <Product>FurkanTural.Labs.Host</Product>
    <Description>Shared in-process web host for the labs that measure request-pipeline behaviour.</Description>
  </PropertyGroup>

  <!-- Assembly information -->
  <PropertyGroup>
    <AssemblyTitle>FurkanTural.Labs.Host</AssemblyTitle>
    <AssemblyDescription>Starts a minimal ASP.NET Core application on an ephemeral port and hands back a client bound to it.</AssemblyDescription>
    <RootNamespace>FurkanTural_Labs_Host</RootNamespace>
  </PropertyGroup>

  <!-- Versioning -->
  <PropertyGroup>
    <Version>1.0.0</Version>
  </PropertyGroup>

  <!-- Framework References -->
  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <!-- Project References -->
  <ItemGroup>
    <ProjectReference Include="..\..\Signature\FurkanTural_Labs_Application\FurkanTural_Labs_Application.csproj" />
  </ItemGroup>
</Project>
